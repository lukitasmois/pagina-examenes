{
  "name": "pagina-examenes",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd ./backend && nodemon index.js\" \"cd ./frontend && npm run dev\"",
    "setup": "npm install && cd ./backend && npm install && cd .. && cd ./frontend && npm install && cd .."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^9.1.2",
    "dotenv": "^16.5.0",
    "nodemon": "^3.1.10"
  }
}
